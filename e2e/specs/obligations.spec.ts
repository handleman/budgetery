import { test } from '../fixtures/test';

/**
 * Obligation Tracking usecases (docs/design/usecases.md):
 *  B1 add fixed obligation
 *  B2 percentage obligation via obligation-percentage-switch
 *  B3 no timestamps shown on rows
 *  B4 daily + remaining recalculated
 *
 * Placeholder: see E2E_PLAYWRIGHT_DESIGN.md §8.
 */
test.describe.skip('obligations', () => {
  test('B1: add fixed obligation', async () => {});
  test('B2: add percentage obligation', async () => {});
  test('B3: rows show no timestamps', async () => {});
  test('B4: daily + remaining recalculated', async () => {});
});
